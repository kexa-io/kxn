{{- if .Values.logs.enabled }}
{{- /*
kxn-logs — separate Deployment that runs the Rust binary in stream mode
to fan-in pod logs from every namespace into the configured save backend
(postgres / loki / mongo). Same image as kxn-monitor, just a different
command. No Python anywhere.
*/ -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-kxn-logs-config
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  kxn.toml: |
    [rules]
    min_level = 0

    [[targets]]
    name = "k8s-logs"
    provider = "kubernetes"
    interval = 30

    [[save]]
    {{- if eq .Values.backend.type "postgres" }}
    type = "postgres"
    url = "KXN_PG_URL"
    {{- else if eq .Values.backend.type "loki" }}
    type = "loki"
    url = "loki://{{ .Release.Name }}-loki:3100"
    compression = "gzip"
    {{- else if eq .Values.backend.type "mongo" }}
    type = "mongo"
    url = "MONGO_URL"
    {{- end }}
    origin = "kxn-stack-logs"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-kxn-logs
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: logs
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: logs
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: logs
    spec:
      serviceAccountName: kxn-stack-kxn-monitor
      containers:
        - name: kxn-logs
          image: "{{ .Values.logs.image.repository }}:{{ .Values.logs.image.tag }}"
          imagePullPolicy: {{ .Values.logs.image.pullPolicy }}
          command:
            - /usr/local/bin/kxn
            - logs
            - kubernetes://in-cluster
            - --stream
            - --stream-batch-lines
            - {{ .Values.logs.streamBatchLines | quote }}
            - --stream-batch-ms
            - {{ .Values.logs.streamBatchMs | quote }}
            - --config-file
            - /config/kxn.toml
          env:
            - name: RUST_LOG
              value: "info"
            {{- if eq .Values.backend.type "postgres" }}
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kxn-stack-postgresql
                  key: password
            - name: KXN_PG_URL
              value: "postgres://kxn:$(PG_PASSWORD)@kxn-stack-postgresql:5432/kxn"
            {{- else if eq .Values.backend.type "mongo" }}
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-mongo
                  key: password
            - name: MONGO_URL
              value: "mongodb://kxn:$(MONGO_PASSWORD)@{{ .Release.Name }}-mongo:27017/kxn?authSource=kxn"
            {{- end }}
          resources:
            {{- toYaml .Values.logs.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ .Release.Name }}-kxn-logs-config
{{- end }}
